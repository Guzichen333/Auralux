# Benchmark audio fixtures

Large benchmark audio files are intentionally not committed in the Auralux publishing snapshot.

Regenerate them with `node scripts/benchmarks/generate-benchmark-fixtures.js` when benchmark runs need local fixtures.
